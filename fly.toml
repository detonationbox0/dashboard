# fly.toml app configuration file generated for dashboard-bitter-star-8601 on 2026-01-24T16:07:53-05:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'dashboard-bitter-star-8601'
primary_region = 'iad'

[build]

[http_service]
  # Fly routes incoming HTTPS traffic to this internal port.
  internal_port = 8080
  force_https = true
  # Scale to zero when idle, wake on request.
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  # Lightweight VM sizing for this app.
  memory = '256mb'
  cpus = 1
  memory_mb = 256
